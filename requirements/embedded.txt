# Embedded requirements
# 
# Typical command to install:
# python -m pip install --no-deps -U -r requirements/embedded.txt -t ext_libs
# -----------------------

dulwich>=0.20,<0.21
giturlparse>=0.9,<0.10 
pathvalidate>=2.3,<2.4
